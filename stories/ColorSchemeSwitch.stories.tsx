import type { Meta } from "@storybook/preact";
import { ColorSchemeSwitch } from "../src/main";

const meta: Meta<typeof ColorSchemeSwitch> = {
  component: ColorSchemeSwitch,
};

export default meta;

export const Default = {};
